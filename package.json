{
  "name": "resonance-web-admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run dev:admin & bun run dev:client",
    "dev:admin": "cd packages/web-admin && bun run dev",
    "dev:client": "cd packages/web-client && bun run dev",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}